<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tyohaarconsole">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Shop_Staff"></activity>
        <activity android:name=".OFFICE_PROC_Supervisor" />
        <activity android:name=".FC_Vehicle_Driver" />
        <activity android:name=".FC_Supervisor" />
        <activity android:name=".CC_Vehicle_Driver" />
        <activity android:name=".CC_Supervisor" />
        <activity android:name=".CC_Proc_Lead" />
        <activity android:name=".APMC" />
        <activity android:name=".Admin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>